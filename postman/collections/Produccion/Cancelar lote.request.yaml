$kind: http-request
name: Cancelar lote
url: "{{baseUrl}}/api/v1/lotes/{{loteId}}/cancelar"
method: PATCH
headers:
  Accept: application/json
scripts:
  - type: afterResponse
    code: |-
      pm.test('status 200', function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 3500

